import React, { Component } from 'react';

class SendMessage extends Component {
  render() {
    return (
      <div className="send">
        <h3 className="type-message">Type message & hit ENTER</h3>
      </div>
    );
  }
}

export default SendMessage;
