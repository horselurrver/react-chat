import React, { Component } from 'react';

class SendMessage extends Component {
  render() {
    return (
      <div className="send">
        <textarea>
        </textarea>
      </div>
    );
  }
}

export default SendMessage;
